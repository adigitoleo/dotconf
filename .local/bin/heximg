#!/bin/sh
set -eu
if [ $# -eq 0 ]; then
    IFS= read -r val && set -- "$val"
elif [ "$1" = "-" ]; then
    IFS= read -r val && set -- "$val"
fi
convert -size 20x20 xc:"$1" /tmp/c.png || exit 1
xdg-open /tmp/c.png
rm /tmp/c.png
