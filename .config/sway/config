# SWAY CONFIG
# Requirements:
# - wld script, <https://git.sr.ht/~adigitoleo/dotconf/tree/master/item/.local/bin/wld>
# - autotiling plugin, <https://github.com/nwg-piotr/autotiling>
# - swaylock <https://github.com/swaywm/swaylock>
# - swayidle <https://github.com/swaywm/swayidle>
# Read `man 5 sway` for a complete reference.

font monospace bold 11
focus_follows_mouse no
seat * hide_cursor when-typing enable
default_border pixel 3
default_floating_border pixel 3
client.focused #BF472C #BF472C #F5BB89 #BF472C
client.focused_inactive #66292F #66292F #BF472C #66292F
client.unfocused #66292F #66292F #F5BB89 #66292F
client.urgent #FF7477 #FF7477 #AF0032 #FF7477

### Variables
set $mod Mod4
set $left h
set $down j
set $up k
set $right l
set $term alacritty
set $lock 'swaylock -c 3d241f'
set $floatterm 'alacritty --class floating-terminal,floating-terminal'
set $menu 'alacritty --class floating-terminal,floating-terminal -e fzfmenu.sh'
set $open 'alacritty --class floating-terminal,floating-terminal -e fzfopen.sh'
set $music 'alacritty --class floating-terminal,floating-terminal -e mocp'

### Window rules
for_window [app_id="drracket"] floating enable
for_window [app_id="floating-terminal"] floating enable
for_window [app_id="gimp"] floating enable
for_window [app_id="gov-nasa-giss-panoply-Panoply"] floating enable
for_window [app_id="imv"] floating enable
for_window [app_id="inkscape"] floating enable
for_window [app_id="jupyter-qtconsole"] floating enable
for_window [app_id="libreoffice"] floating enable
for_window [app_id="matplotlib"] floating enable
for_window [app_id="mpv"] floating enable
for_window [app_id="mscore"] floating enable
for_window [app_id="mypaint"] floating enable
for_window [app_id="qt5ct"] floating enable
for_window [app_id="zathura"] floating enable

### Input configuration
# Read `man 5 sway-input` for a complete reference.
input * xkb_layout us
input * xkb_variant altgr-intl
input * xkb_options altwin:swap_lalt_lwin,ctrl:swapcaps
input type:touchpad tap enabled
input type:touchpad natural_scroll enabled

### Key bindings
bindsym $mod+Shift+Return exec $term
bindsym $mod+Shift+c kill
bindsym $mod+Shift+d exec $menu
bindsym $mod+Shift+o exec $open
bindsym $mod+Shift+m exec $music
bindsym $mod+Shift+f exec $floatterm

floating_modifier $mod normal
bindsym $mod+Shift+r reload
bindsym $mod+Shift+q exit
bindsym $mod+Shift+l exec $lock

# Move your focus around
bindsym $mod+$down exec ~/.config/sway/wincycle.py next
bindsym $mod+$up exec ~/.config/sway/wincycle.py prev
bindsym $mod+comma focus output right
# Or use $mod+[up|down|left|right]
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move the focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right
# Or mark a window and then swap positions with it
bindsym $mod+m mark --add swapmark
bindsym $mod+Shift+s swap container with mark swapmark;\
    [con_mark="swapmark"] focus; unmark swapmark

# Switch to workspace
bindsym $mod+n workspace next_on_output
bindsym $mod+p workspace prev_on_output
bindsym $mod+Tab workspace back_and_forth
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10
# Move focused container to workspace
bindsym $mod+Shift+comma move container to output right
bindsym $mod+Shift+Tab move container to workspace back_and_forth
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# Layout stuff
bindsym $mod+f fullscreen
bindsym $mod+t layout toggle split tabbed
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
bindsym $mod+h focus parent
bindsym $mod+l focus child
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Status Bar:
# Read `man 5 sway-bar` for more information about this section.
bar {
    # When the status_command prints a new line to stdout, swaybar updates.
    status_command while wl-bar.sh; do sleep 1; done

    colors {
        font monospace bold
        statusline #F5BB89
        background #66292F
        focused_workspace #F5BB89 #F5BB89 #66292F
        active_workspace #A67458 #A67458 #66292F
        inactive_workspace #66292F #66292F #F5BB89
        urgent_workspace #FF7477 #FF7477 #AF0032
    }
}

exec --no-startup-id wld
exec swayidle -w timeout 600 $lock before-sleep $lock
exec_always --no-startup-id autotiling
include /etc/sway/config.d/*
